/*
CSCI 104: Homework 1 Problem 1

Use this file to test your split.cpp file.

Create linked lists and split them with your split() function.

You can compile this file like this:
g++ split.cpp test_split.cpp -o test_split
*/

#include "split.h"
#include <iostream>
using namespace std;

int main(int argc, char* argv[])
{
  /*Node* list1 = new Node(2, new Node(4, new Node(5, new Node(6,nullptr))));
  Node* list2 = new Node(1, new Node(2, new Node(3, new Node(4,nullptr))));
  Node* odd = nullptr;
  Node* even = nullptr;
  
  split(list2, odd, even);
  Node* currentEven = even;
  cout << "This is even: " << endl;
  while (currentEven!=nullptr){
    cout << currentEven->value << endl;
    currentEven = currentEven->next;
  }
  cout << "This is odd: " << endl;
  Node* currentOdd = odd;
  while (currentOdd!=nullptr){
    cout << currentOdd->value << endl;
    currentOdd = currentOdd->next;
  }
  delete list1;
  delete list2;*/
}
